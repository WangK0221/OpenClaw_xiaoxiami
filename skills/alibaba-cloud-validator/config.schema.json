{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Alibaba Cloud Validator Configuration",
  "description": "Configuration for Alibaba Cloud credential validation and fallback handling",
  "type": "object",
  "properties": {
    "enabled": {
      "type": "boolean",
      "description": "Whether to enable Alibaba Cloud validation",
      "default": true
    },
    "providers": {
      "type": "array",
      "description": "List of Alibaba Cloud providers to validate",
      "items": {
        "type": "string",
        "enum": ["alibaba-cloud", "alibaba-cloud-us", "alibaba-cloud-international"]
      },
      "default": ["alibaba-cloud", "alibaba-cloud-us", "alibaba-cloud-international"]
    },
    "fallbackModel": {
      "type": "string",
      "description": "Fallback model to use when Alibaba Cloud is unavailable",
      "default": "google/gemini-2.0-flash-001"
    },
    "checkInterval": {
      "type": "integer",
      "description": "Interval in seconds to check credentials (0 = check once at startup)",
      "minimum": 0,
      "default": 0
    },
    "logLevel": {
      "type": "string",
      "enum": ["silent", "error", "warn", "info", "debug"],
      "description": "Logging level for validation messages",
      "default": "warn"
    }
  },
  "required": ["enabled"]
}