# Exec Tool Optimizer

Optimizes exec tool usage by providing proper PATH resolution and error handling for system commands.

## Features
- Automatic PATH resolution for OpenClaw and other executables
- Graceful fallback when executables are not found in expected locations
- Enhanced error reporting with actionable suggestions
- Integration with existing tool execution patterns

## Usage
The optimizer automatically enhances all exec tool calls without requiring code changes.